---
description: 変更完了時に必ずコミット＆プッシュする
---

# コミット＆プッシュ手順

変更を完了したら、**必ず**以下を実行すること。

// turbo-all

1. `cd /Users/hoshinoyoshiki/Documents/MAJAN_Rule && git add -A && git status --short`
2. 変更内容を簡潔に日本語でまとめたコミットメッセージを作成し、`git commit -m "メッセージ"` を実行
3. `git push` を実行
